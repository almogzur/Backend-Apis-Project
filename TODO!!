deploy // aws  dns =>
 aws cloudeflare =>
  aws R53 => 
  virtual privert cloud =>
   static ip=> enstence
    =>unutu => node =>
    fix port 80 premition  =>
    add https  greenlock  =>
    auto sync repo // intalled git gub on server // can be sync from post recive
    texting pashe 
change app render to match project needs 
add link to api 
render exmple whit json.stringfiy


